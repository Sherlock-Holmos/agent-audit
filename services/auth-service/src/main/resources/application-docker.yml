spring:
  datasource:
    url: jdbc:mysql://mysql:3306/agent_audit?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
  sql:
    init:
      mode: always
      continue-on-error: true
